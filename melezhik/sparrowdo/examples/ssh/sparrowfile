my $user = input_params('SshUser');

ssh q:to/HERE/, %( host => "127.0.0.1" , user => $user, ssh-key =>  "/home/$user/.ssh/id_rsa" );
  echo one
  echo two
  echo three
HERE

ssh 'uptime', %( host => "127.0.0.1" , user => $user, ssh-key =>  "/home/$user/.ssh/id_rsa" );
