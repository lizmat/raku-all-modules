#!/usr/bin/env perl6
use v6.c;
use Module::Skeleton;

sub MAIN($name, $root? is copy, Str :$author?, Str :$description?) {
    $root //= $name.subst('::', '-', :g);
    $root .= IO;

    my $skeleton = Module::Skeleton.new(:$name, :$author, :$description);
    $skeleton.spurt($root);
}
